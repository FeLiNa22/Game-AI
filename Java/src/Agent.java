import Game.Board;
import Game.Move;
import Game.Player;

public class Agent extends Player{

  public Agent(String name) {
    super(name);
  }

  @Override
  public Move getMove(Board b) {
    return super.getMove(b);
  }
}
